# 扫描结果独立抽查报告

对 `scan_summary.json` 的判定结果做**独立回查**：不依赖工具自身的结论，直接重新抓取
PyPI / npm 的原始元数据，逐条人工比对。

- 抽查时间：2026-09-25（**v0.4.3 复核**，本次修复了脚本的两处缺陷，见文末）
- 抽样方式：分层随机（固定随机种子 `20260924`，可复现）
- 样本构成：已识别 / 传染性许可 / **未识别** 三层各约 1/3，共 **40 条**

## 结论

> ⚠️ **口径说明（v0.4.1 起）**：下表是**宽口径**结果。此前报告只写「准确率 100%」
> 而未说明口径，掩盖了一个事实——宽口径允许「族 + 版本号」模糊匹配，
> 并允许 `ALIAS` 表预映射后再比对，而 `ALIAS` 表本身是工具的既有知识，
> 拿它来比对带有自证成分。v0.4.1 起 `verify_sample.py` 同时输出**严口径**
> （只做机械规范化，不查任何映射表）与**宽口径**两个数字，定义见脚本头部注释。

| 指标 | 数量 |
|---|---|
| 抽样总数 | 40 |
| 有效可比对 | 27 |
| 严口径一致（规范化后字面相同） | 18 |
| 仅宽口径一致（族+版本/别名匹配） | 4 |
| 家族已识别、版本待确认（`*-unknown`） | 3 |
| 源站有值但工具未识别 | 2 |
| 不一致 | 0 |
| **严口径准确率** | **18 / 27 = 66.7%** |
| **宽口径准确率** | **25 / 27 = 92.6%** |

未纳入比对的 13 条：源站无此包 **0**、源站无许可证字段 8、**抓取失败 5**。

### 结论的稳定性（两次独立运行）

抓取失败率随网络状况变化，所以**有效样本数每次运行都不同**。为验证结论不受影响，
在修复后连续跑了两次：

| 运行 | 抓取失败 | 有效可比对 | 严口径 | 宽口径 | 不一致 |
|---|---|---|---|---|---|
| 第 1 次（本表采用） | 5 条 | 27 | 66.7% | 92.6% | 0 |
| 第 2 次 | 7 条 | 25 | 68.0% | 92.0% | 0 |

两次的差异只来自哪几条恰好抓取成败，**已比对部分的判定结果完全一致**
（`demjson3` 在第 2 次抓取失败，第 1 次它是「仅宽口径一致」）。
这正是把抓取失败单列成桶之后才具备的性质：网络抖动不再污染结论。

## 需要人工裁决的条目

自动比对脚本的机械匹配会漏判，逐条查证原始值后确认工具判定全部正确。

**4 条「仅宽口径一致」——归一化能力的体现，不是判定错误**

| 包名 | 源站原始值 | 工具判定 | 说明 |
|---|---|---|---|
| `fuzzywuzzy` | trove: `GNU General Public License v2 (GPLv2)` | `GPL-2.0-only` | `v2` 无 `or-later` 后缀，判为 `-only` 语义准确 |
| `es-core-news-sm` | `GNU GPL 3.0` | `GPL-3.0-only` | 自然语言写法成功归一化 |
| `nltk` | `Apache Software License` | `Apache-2.0` | 源站给的是非 SPDX 的自然语言写法 |
| `demjson3` | `GNU Library or Lesser General Public Lic…` | `LGPL-3.0-only` | 被截断的自然语言，归一化到正确版本 |

> 严口径只衡量"字面是否对得上"，因此这类条目会被严口径判为不一致。两个口径
> 各自回答不同的问题：严口径问"有没有字面依据"，宽口径问"判定是否正确"。

**3 条「家族已识别、版本待确认」——克制的正确行为**

`fr-core-news-sm`（`LGPL-LR`）、`extract-msg`（`GPL`）、`psycopg2`
（`GNU Library or Lesser General Public Lic…`）源站给的都是**含义不明或未带版本号**
的写法，工具标为 `*-unknown` 而不是猜一个版本。这正是 README「已知限制」里承诺的
行为——**不确定就标待确认，不假装确定**。

**2 条「源站有值但工具未识别」——按主程序口径属于「源站未填许可证」**

| 包名 | 源站原始值 | 工具判定 | 说明 |
|---|---|---|---|
| `rouge` | `LICENCE.txt` | `UNKNOWN` | 源站把**文件名**填进了 license 字段 |
| `nvidia-sphinx-theme` | `Other/Proprietary License` | `UNKNOWN` | 非许可证标识的占位描述 |

这两条在抽查脚本里被归入「源站有值但工具未识别」，但在主程序里会被
`is_placeholder_license()` 判为**占位值**，归因 `NO_METADATA`（源站没给数据）
而非工具的漏判。**两个口径并不矛盾**：抽查脚本刻意不 import `license_audit`，
以免"用工具验证工具"，因此它只做字面层面的机械比对，不做占位值识别；
主程序则在此之上多一层归因。展示上两者都要看——这正是把归因拆成四类的原因。

## 一处口径缺陷已修正（v0.4.1 起，v0.4.2 补完）

此前 `loose_match` 对工具判定为 `UNKNOWN` 的条目**直接返回一致**（理由是"源站信息
不足时不臆断是正确行为"）。但这条规则用错了地方：当**源站明确给了许可证值、
工具却没能识别**时，那是一次真实的漏判，却被计入了一致，从而抬高了准确率。

v0.4.1 起该分支被移出主流程，单列为「源站有值但工具未识别」桶；
v0.4.2 进一步把 `loose_match` 内部那个已不可达的同类宽容分支直接删除，
避免读者误以为 UNKNOWN 仍会被计为一致（检查清单 P3-3）。两个口径都不计入一致。

## v0.4.3 修正的两处缺陷（本次复核发现）

本报告的用途是"证明扫描结论可信"，所以它自身的缺陷同样要摆出来。

### 1. 网络失败曾被说成「源站无此包」

`upstream_values()` 原先用 `except Exception: return None` 把所有异常统一吞成
`None`，调用方再把 `None` 读作"源站无此包"。于是超时、连接中断、限流都被
**说成了源站的问题**——而单独 `curl` 那些包，拿到的是连接失败（状态码 000）
而不是 404。最能说明问题的一条：同一批抽样里 `tqdm` 出现 5 次，
**两次取到 `MPL-2.0 AND MIT`，三次报"源站无此包"**。

现在区分三种状态并分开计数，与主程序 `license_audit.py` 的
`NOT_FOUND` / `NO_METADATA` / `FETCH_FAILED` 归因保持同一套语义：

| 状态 | 含义 | 建议 |
|---|---|---|
| 源站无此包 | HTTP 404，源站确实没有 | 核对包名拼写 / 是否私有包 |
| 源站无许可证字段 | 有包但未填许可证 | 人工核对上游仓库 LICENSE 文件 |
| 抓取失败 | 超时 / 连接中断 / 限流 | **重跑即可**，不计入可比对基数 |

抓取同时加了 3 次重试；抓取失败占比偏高时脚本会明确提示"本次准确率不可用"。

### 2. 分层抽样的「未识别层」从未抽到过任何记录

未识别在数据里是**字符串** `"UNKNOWN"` 而非空值。原判定写成
`not rec.get("spdx")`，而 `"UNKNOWN"` 是 truthy——于是库里 19 条未识别记录
**全部被归进「已识别」层**，未识别层恒为空，`--n 40` 实际只抽到 26 条。

后果是这层被系统性回避，而它恰恰是最该审视的部分：工具说"认不出来"，
到底是源站没给数据，还是知识库漏收了？修复后未识别项首次进入样本，
宽口径准确率随之从 **100%（24/24）回落到 92.6%（25/27）**——
**这个下降不是退步，是此前被掩盖的情况终于可见**。

## 复现方式

```bash
# 1. 重新扫描（产物 scan/ 与 scan_summary.json）
python scan_projects.py --jobs 16

# 2. 独立回查：直接抓源站元数据比对
python verify_sample.py
```

`verify_sample.py` 仅依赖 Python 标准库，固定随机种子，结果可复现。
运行后会分别打印严口径与宽口径两个准确率，并单列「源站无此包」
「源站无许可证字段」「抓取失败」「源站有值但工具未识别」几类，
便于区分"判定错误""口径差异"与"本次没抓到"。

> 注意：**抓取失败条数随网络状况波动**，因此有效样本数每次运行不同。
> 脚本会在抓取失败占比偏高时提示本次准确率不可用；引用数字时请连同
> 该次运行的抓取失败条数一起给出。
