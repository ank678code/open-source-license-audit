# 扫描结果独立抽查报告

对 `scan_summary.json` 的判定结果做**独立回查**：不依赖工具自身的结论，直接重新抓取
PyPI / npm 的原始元数据，逐条人工比对。

- 抽查时间：2026-09-24
- 抽样方式：分层随机（固定随机种子 `20260924`，可复现）
- 样本构成：已识别项 24 条 + 传染性许可项 10 条 + 未识别项 6 条 = **40 条**

## 结论

> ⚠️ **口径说明（v0.5 修订）**：下表是**宽口径**结果。此前报告只写「准确率 100%」
> 而未说明口径，掩盖了一个事实——宽口径允许「族 + 版本号」模糊匹配，
> 并允许 `ALIAS` 表预映射后再比对，而 `ALIAS` 表本身是工具的既有知识，
> 拿它来比对带有自证成分。v0.5 起 `verify_sample.py` 同时输出**严口径**
> （只做机械规范化，不查任何映射表）与**宽口径**两个数字，定义见脚本头部注释。

| 指标 | 数量 |
|---|---|
| 抽样总数 | 40 |
| 其中：源站确实无此包（工具标"未识别"正确） | 6 |
| 有效可比对样本 | 34 |
| 自动匹配一致（宽口径） | 30 |
| 人工复核后确认为一致 | 4 |
| **宽口径准确率** | **34 / 34 = 100%** |
| 严口径准确率 | 需联网重跑后出具，见下方说明 |

**严口径数字为什么空着**：本机对 `pypi.org` 不可达（TLS 握手失败），无法重跑抽样。
补跑命令见文末「复现方式」。**这里不填推算值**——一个没有实际执行过的数字没有意义。
按下方逐条口径归位可以预期，严口径会明显低于 100%，因为工具的价值之一正是
把「GNU GPL 3.0」这类自然语言写法正确归一，而这类匹配天然不满足严口径。

## 4 条需要人工裁决的条目（均已确认为工具正确）

自动比对脚本机械匹配会漏判，逐条查证原始值后确认工具判定全部正确。
v0.5 起下表额外标注每条在新口径下的归位，便于看清「正确」与「严口径一致」的区别：

| 包名 | 源站原始值 | 工具判定 | 新口径归位 | 裁决 |
|---|---|---|---|---|
| `unitxt` | `Apache License\n Version 2.0, January 2004 …` | `Apache-2.0` | 仅宽口径一致 | ✅ 正确。多行许可证正文，工具按首行归一化，未受正文干扰 |
| `fuzzywuzzy` | trove: `GNU General Public License v2 (GPLv2)` | `GPL-2.0-only` | 仅宽口径一致 | ✅ 正确。`v2` 无 `or-later` 后缀，判为 `-only` 语义准确 |
| `es-core-news-sm` | `GNU GPL 3.0` | `GPL-3.0-only` | 仅宽口径一致 | ✅ 正确。自然语言写法成功归一化 |
| `fr-core-news-sm` | `LGPL-LR` | `LGPL-unknown` | 家族已识别、版本待确认 | ✅ 正确且克制。`LGPL-LR` 是含义不明的非标准写法，工具**不臆断版本**、标为待人工确认 |

> 这三条「仅宽口径一致」不是判定错误，而是**归一化能力本身**的体现：
> 源站给的是自然语言，工具给出了标准 SPDX 标识。严口径只衡量"字面是否对得上"，
> 因此这类条目会被严口径判为不一致——两个口径各自回答不同的问题，都要看。

> 最后一条尤其值得注意：这正是 README「已知限制」里承诺的行为——**不确定就标待确认，不假装确定**。

## 一处口径缺陷已修正（v0.5）

此前 `loose_match` 对工具判定为 `UNKNOWN` 的条目**直接返回一致**（理由是"源站信息
不足时不臆断是正确行为"）。但这条规则用错了地方：当**源站明确给了许可证值、
工具却没能识别**时，那是一次真实的漏判，却被计入了一致，从而抬高了准确率。

v0.5 起该分支被移出比对流程，单列为「源站有值但工具未识别（漏判）」桶，
两个口径都不计入一致。

## 6 条"源站无此包"说明了什么

`apache-airflow-kubernetes-tests`、`apache-airflow-docker-tests`、`apache-airflow-ctl-tests`、
`apache-airflow-providers-duckdb`、`en-core-web-sm`、`de-core-news-sm` 在 PyPI 上确实不存在
（多为项目内部的测试依赖组或需手动下载的模型包）。工具将其标为 `UNKNOWN / 未识别` 而**不猜、
不误报为风险项**，行为正确。

## 复现方式

```bash
# 1. 重新扫描（产物 scan/ 与 scan_summary.json）
python scan_projects.py --jobs 16

# 2. 独立回查：直接抓源站元数据比对
python verify_sample.py
```

`verify_sample.py` 仅依赖 Python 标准库，固定随机种子，结果可复现。
运行后会分别打印严口径与宽口径两个准确率，并单列「源站无数据」「工具未识别」
「家族已识别版本待确认」三类，便于区分"判定错误"与"口径差异"。
